# Copy to .env (gitignored) and fill in. Loaded automatically by `npm run dev:api` / `npm start`.
GEMINI_API_KEY=your-key-from-aistudio.google.com
# GEMINI_MODEL=gemini-flash-lite-latest
# RATE_LIMIT_MAX=20
# RATE_LIMIT_WINDOW_MS=3600000
